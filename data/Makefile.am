if ENABLE_DEMO
SUBDIRS=ui
endif

mimedir = $(datadir)/mime/packages
mime_DATA = libfm.xml

libfmconfdir = $(sysconfdir)/xdg/libfm
libfmconf_DATA = libfm.conf

EXTRA_DIST= \
	$(libfmconf_DATA) \
	$(mime_DATA)

